import React from "react";
import { Typography, Grid } from "@mui/material";
const ViewAssignedTokens = () => {
  return (
    <Grid>
      <Typography>
        Assigned Tokens data for Distributer to be displayed here
      </Typography>
    </Grid>
  );
};
export default ViewAssignedTokens;
